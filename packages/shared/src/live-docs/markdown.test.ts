import { describe, it } from "vitest";

// Placeholder suite retained while integration coverage replaces the former unit tests.
// This ensures Vitest recognises the module without duplicating assertions exercised
// elsewhere. Remove once the file itself is deleted in a future cleanup pass.
describe("live-docs markdown placeholder", () => {
  it("marks placeholder suite", () => {
    // Intentionally empty.
  });
});
